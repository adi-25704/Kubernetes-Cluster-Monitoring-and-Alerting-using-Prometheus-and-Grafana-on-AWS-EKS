alertmanager:
  config:
    global:
      resolve_timeout: 5m
    route:
      group_by: ['job']
      group_wait: 30s
      group_interval: 5m
      repeat_interval: 12h
      receiver: 'sns-forwarder' 
      routes:
      - match:
          alertname: Watchdog
        receiver: 'null'
    receivers:
    - name: 'null'
    - name: 'sns-forwarder'
      webhook_configs:
      
      - url: 'http://prometheus-sns-forwarder.monitoring.svc:9087/alert'
        send_resolved: true